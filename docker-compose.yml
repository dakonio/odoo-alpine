services:

  odoo19-ce-db:
    image: postgres:15
    container_name: odoo19-ce-db
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - odoo19-ce-db-data:$PGDATA

  odoo19-ce-erp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: odoo19-ce-erp
    env_file:
      - .env
    tty: true
    depends_on:
      - odoo19-ce-db
    platform: linux/amd64

  odoo19-ce-web:
    image: nginx
    container_name: odoo19-ce-web
    ports:
      - "80:8080"
    depends_on:
      - odoo19-ce-erp
    volumes:
      - ./etc/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  odoo19-ce-db-data:
    driver: local
