services:

  odoo18-ce-db:
    image: postgres:15
    container_name: odoo18-ce-db
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - odoo18-ce-db-data:$PGDATA

  odoo18-ce-erp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: odoo18-ce-erp
    env_file:
      - .env
    tty: true
    depends_on:
      - odoo18-ce-db
    platform: linux/amd64

  odoo18-ce-web:
    image: nginx
    container_name: odoo18-ce-web
    ports:
      - "80:8080"
    depends_on:
      - odoo18-ce-erp
    volumes:
      - ./etc/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  odoo18-ce-db-data:
    driver: local
